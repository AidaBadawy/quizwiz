enum TabItemEnum {
  home,
  explore,
  events,
  profile,
}

enum StatusEnum {
  busy,
  idle,
  ready,
  started,
}

enum AlignmentEnum {
  right,
  left,
}

enum CategoryType {
  explore,
  event,
}

enum MessageType {
  text,
  image,
  audio,
  video,
  log,
}

enum NotificationType {
  mention,
  comment,
  request,
}

enum TripType {
  personal,
  group,
}
